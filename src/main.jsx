import React from 'react'
import ReactDOM from 'react-dom/client'
import PosterSelect from './PosterSelect.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PosterSelect />
  </React.StrictMode>,
)
